{
  "$schema": "https://schema.tauri.app/config/2",
  "build": {
    "beforeDevCommand": "npm run build:helper:dev && npm run dev",
    "beforeBuildCommand": "npm run build:helper && npm run build"
  },
  "bundle": {
    "externalBin": ["binaries/slovo-input-helper"],
    "resources": ["resources/72-slovo-input-helper.rules"]
  }
}
