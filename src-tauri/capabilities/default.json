{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal capability for the Slovo settings window",
  "windows": ["main"],
  "permissions": ["core:default"]
}
